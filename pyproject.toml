[project]
name = "ticket-triage"
version = "0.1.0"
requires-python = ">=3.11"
[build-system]
description = "Production-grade MLOps service that classifies customer support tickets."
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
where = ["src"]