global:
  scrape_interval: 15s

scrape_configs:
  - job_name: automated-support-ticket-classification
    metrics_path: /metrics
    static_configs:
      # "api" is the compose service name. Inside the compose network
      # localhost would mean the Prometheus container itself, not the API.
      - targets: ["api:8000"]
