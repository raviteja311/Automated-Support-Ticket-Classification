# Runtime dependencies (known-good versions; pip may offer newer ones)
scikit-learn==1.5.2
pandas==2.2.3
joblib==1.4.2
PyYAML==6.0.2
pydantic==2.9.2
fastapi==0.115.5
uvicorn[standard]==0.32.1
mlflow==2.18.0
dvc==3.55.2
prometheus-fastapi-instrumentator==7.0.0

# Transitive pin. dvc 3.55.2 imports _DIR_MARK from
# pathspec.patterns.gitwildmatch, which pathspec 1.x moved to
# pathspec.patterns.gitignore.spec. Without this pin pip resolves
# pathspec 1.x and every dvc command fails on import.
pathspec==0.12.1

# scipy 1.18 dropped the lbfgs "iprint" option that every scikit-learn
# release still sends, producing an OptimizeWarning on every model fit.
# Verified: iprint is accepted through scipy 1.17.0 and gone in 1.18.1.
scipy<1.18
numpy<3